import React, { Component } from 'react'
import colors from '../styles/allTheColors.js'

/*
if Home or About or Contact or whatever is clicked, this child component is responsible for sending back to the parent the new display properties of the main-box, i.e. a modal 'pop-up' style window, leaving the grey in the background but itself being styled by whichever color scheme was clicked on, or maybe randomly generated?
*/

export default class MainChange extends Component {
    constructor(props) {
        super(props)
    }

    render() {
        return(

        )
    }
}
